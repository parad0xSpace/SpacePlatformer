using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class SceneTracker : MonoBehaviour
{
    private bool hasCheckedScene;

    private int levelNumber;

    public GameObject player;

    void Start()
    {
        //set level numver coorilating to scene number
    }

    void Update()
    {
        //check scene, set hasCheckedScene to true
        //if (scene number), set scene to scene number
    }
}
